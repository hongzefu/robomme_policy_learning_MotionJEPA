# v5.1 计划：60k 全量训练提交——wandb 新 project + framesamp-context + 集群侧上游对拍

> **本文件是 v5.1 工作的权威计划**（2026-08-30 定稿，待用户批准后实施）。
> **锚点**：分支 `v1-dataloader-Restructure`，起点 HEAD = `ae39c75`（v5.0 四步全绿收官：
> C2 `f641f40` / C2.5 RECORDER_SMOKE / C3 G0_EQ=PASS / C4 ENTRY_EQ=PASS）。
> **在途改动申明**：`scripts/training/train.py` 的 wandb entity 一行已先行修改（工作区
> 在途、未提交，详见文末），获批随 commitV5.1 提交、被否回退。
> **commit 编号**：代码切片为 **commitV5.1**；两个集群 run 拟名 `v1-gl-entry-eq`（D2）
> 与 `v1-prod-60k`（D3），起跑前按 AGENTS 6 逐一确认。

# 第一部分（给人看）

## Context：为什么做这件事

v5.0 入口重构已全绿收官：新 `scripts/training/train.py`（单跑）与上游 main `ecf086c` 官方入口经本机 1000 步位级对拍双侧命中锚点（`ENTRY_EQ=PASS`，`docs/training-doc/v1-upstream-eq/`）。现在提交**首次全量正式训练**：60,000 步、framesamp packed 库 + `perceptual-framesamp-context` 变体、开启 wandb（新 project），并把「与上游原版对拍」延伸到集群 A40 硬件上。

## 用户已拍板项（2026-08-30，本轮 AskUserQuestion 逐项确认）

| 议题 | 拍板 |
|---|---|
| wandb project | **`robomme-framesamp`**（新建，已由本机探针自动建立） |
| wandb entity | **改用个人 entity**：`WANDB_ENTITY` 环境变量覆盖、默认仍 `daiyp_umich`（不设时与上游行为完全一致）。依据：实测用户 key 写 `daiyp_umich` 报 403，写 `hongzefu-university-of-michigan` PASS |
| API key | 用户已在会话提供；**只经环境变量传递（sbatch `--export`），不落任何文件、不进 git** |
| 集群侧对拍（D2） | **A/B 位级对拍，跑 100 步**（用户原话「ab位 跑100step」） |
| ckpt 档位 | **官方口径 save 10000 / keep 10000**（6 个 ckpt ≈ 78 GB，单 ckpt 实测 13 GB） |
| 步数落点 | `STEPS=60000` 走 sbatch 覆盖参数，全局默认不动（AGENTS 10） |

## 关键结论一：wandb 记录与 main branch 一致（已核实）

逐行 diff 本分支 `train.py` 与上游 `ecf086c:scripts/train.py`：`init_wandb` 逐字相同（唯一差异 `log_code` 仓库根路径修正，且默认 `log_code=False` 不触发）；`wandb.log` 调用点逐字相同（step 0 一次 `camera_views` + 每 `log_interval=100` 步一次五标量均值 `reduced_info`）；`TRAIN_RECORD_DIR` 记录器只读转发（C4 位级证明）。**60k 的 wandb 曲线口径与上游官方训练完全同构。** entity 的 env 覆盖只改上报归属，不改上报内容。

## 关键结论二：60k 是官方 80k 的位级前缀

lr schedule 在具名配置里写死（warmup 10k / peak 5e-5 / decay 100k，`src/mme_vla_suite/training/config.py` 的 `CosineDecaySchedule` 字段），**不依赖 `num_train_steps`**——60k 逐步 lr 与官方 80k 的前 60k 步完全一致。超参全取官方具名口径：batch 64 / fsdp 4 / log_interval 100 / save·keep 10000。

## 关键实测底座

- **速率**：`v1-prod-trend-10h` 实测 4×A40/b64 步时均值 4.814 s、util 99.6% 无衰减 → 60k ≈ **80.2 h**，walltime 申请 **96 h**（裕度 1.2×）。
- **磁盘**：turbo 剩 6 TB；6 ckpt ≈ 78 GB。
- **wandb 探针**（本机已跑）：key + entity `hongzefu-university-of-michigan` + project `robomme-framesamp` 全通，探针 run 待清理。

## 三步走

**D1（本机代码，commitV5.1）**：①`train.py` 的 `init_wandb` entity 改 `os.environ.get("WANDB_ENTITY", "daiyp_umich")`（⚠ 此行已在计划批准前动手、现为工作区在途改动，批准后随 commitV5.1 提交，否决则回退）；②`tests/entry_equiv.py` judge 步数参数化 + 锚点可选（A40 上无本机锚点）；③`gl_train_prod.sbatch` 加 `WANDB_PROJECT` 条件块（未设时与现状逐字节等价）；④新增 `tests/gl_entry_eq.sbatch`（A/B 各 100 步 + judge + 计算节点 wandb 探针 + 四段 RC 硬化）；⑤重建上游 worktree + uv sync；⑥秒级闸 + 清理 wandb 探针 run。

**D2（集群 job，拟名 `v1-gl-entry-eq`，提交前另行放行 + Okta）**：单 job 4×A40/16C/128G/3h，串行跑上游原版（tentative 12 步 + 100 步、`--overwrite`、legacy 库）与新入口（100 步、packed 库），生产口径 b64/fsdp4 + 确定性档，judge 互比四项判定（`ENTRY_SCALARS steps=100 hex_mismatch=0` / `ENTRY_STATE_DIGEST`（save-interval 25，约 4 行）/ `ENTRY_RESOLVED_CFG` / `ENTRY_PROVENANCE`）→ `ENTRY_EQ=PASS`；同 job 探针验证计算节点 wandb 出网。⚠ 已知风险：确定性 XLA 档（D2 flags）只在本机 Ada 实测过，A40 首次——若 FAIL 先甄别硬件不确定性（同侧重跑二分）再谈入口差异。留档 `docs/training-doc/v1-gl-entry-eq/`（AGENTS 17）。

**D3（集群 job，拟名 `v1-prod-60k`，起跑前确认 run_name + 放行 + Okta）**：`gl_train_prod.sbatch` 提交，`STEPS=60000 SAVE_INTERVAL=10000 WANDB_PROJECT=robomme-framesamp WANDB_ENTITY=hongzefu-university-of-michigan` + key 经 `--export`；4×A40/16C/128G/**96h**；`TRAIN_RECORD_DIR` 记录器 + 五路采样器 + 退出码硬化首次实战；起跑前 clean HEAD + launch.md 先行提交（AGENTS 12）。

---

# 第二部分（技术细节，供 agent 追踪）

## D1 逐处改动

1. **`scripts/training/train.py`**（已在途）：`init_wandb` 内 `entity="daiyp_umich"` → `entity=os.environ.get("WANDB_ENTITY", "daiyp_umich")`，附中文注释说明 403 实测依据。`os` 已 import（v5.0）。
2. **`scripts/training/tests/entry_equiv.py`**：
   - judge 子命令加 `--expect-steps`（int，默认 1000），替换 `_judge` 里硬编码的 `len(common) != 1000`；
   - `--expect-sha256` 改 `required=False`：缺省时跳过双侧锚点子判据（不打印 anchor_hit 行，改打 `X_SCALARS_SHA256 <sha> anchor=SKIPPED`），`ENTRY_EQ` 定义收缩为其余四项全过。
3. **`scripts/training/prod/gl_train_prod.sbatch`**：现 `export WANDB_MODE=disabled`（无条件）改为条件块：
   ```bash
   if [ -n "${WANDB_PROJECT:-}" ]; then
       [ -n "${WANDB_API_KEY:-}" ] || { echo "错误: 设了 WANDB_PROJECT 但缺 WANDB_API_KEY"; exit 1; }
       export WANDB_MODE="${WANDB_MODE:-online}"
       export WANDB_DIR="$STORE/cache/wandb" WANDB_CACHE_DIR="$STORE/cache/wandb" WANDB_CONFIG_DIR="$STORE/cache/wandb"
       mkdir -p "$WANDB_DIR"
       WANDB_ARGS=(--project-name "$WANDB_PROJECT")     # 不带 --no-wandb-enabled
   else
       export WANDB_MODE=disabled
       WANDB_ARGS=(--no-wandb-enabled)
   fi
   ```
   训练命令行尾部 `--no-wandb-enabled` 换成 `"${WANDB_ARGS[@]}"`；`WANDB_ENTITY`/`WANDB_API_KEY` 经 sbatch `--export` 透传（train.py 直接读）。头注释与 write_env 的 `WANDB_MODE` 记录保持。未设 `WANDB_PROJECT` 时行为与现状逐字节等价（对拍 diff 验证）。
4. **新增 `scripts/training/tests/gl_entry_eq.sbatch`**：
   - `#SBATCH`：`--account=chaijy2 --partition=spgpu --gpus-per-node=4 --cpus-per-task=16 --mem=128G --time=03:00:00`，输出到 `v1-store/logs/%x-%j.log`；
   - 骨架照抄 `gl_train_prod.sbatch`（uv 检测、`run_py`、trap RC 硬化模板 v5.0 版）；
   - 两侧共同 env：`XLA_FLAGS='--xla_gpu_deterministic_ops=true --xla_gpu_autotune_level=0'`、`XLA_PYTHON_CLIENT_MEM_FRACTION=0.95`、jax 缓存软链 `~/.cache/jax_gl-entry-eq-{a,b}` → `$STORE/cache/jax/`；
   - A 侧：`cd $WORKTREE && env -u PYTHONPATH -u PYTHONHOME uv run --no-sync --project "$WORKTREE" python $REPO/scripts/training/tests/entry_equiv.py run --entry $WORKTREE/scripts/train.py --record-dir $STORE/entryeq/records/a --expect-root $WORKTREE --expect-steps 100 -- mme_vla_suite --exp-name gl-entry-eq-a --overwrite --num-train-steps 100 --log-interval 1 --save-interval 25 --batch-size 64 --num-workers 8 --seed 42 --fsdp-devices 4 --dataset-path $STORE/datasets/4task-gl --assets-base-dir $STORE/train-assets --checkpoint-base-dir $STORE/entryeq/ckpt-a --weight-loader.params-path $STORE/models/openpi-assets/checkpoints/pi05_base/params --model.use-history --model.history-config perceptual-framesamp-context.yaml --no-wandb-enabled`；
   - B 侧：入口 `$REPO/scripts/training/train.py`、`--exp-name gl-entry-eq-b`、无 `--overwrite`、packed 库 `4task-gl-framesamp`、`ckpt-b`、`--expect-root $REPO`、`env -u TRAIN_RECORD_DIR`，其余逐字符同（对拍纯净：两侧均 `--no-wandb-enabled`）；
   - judge：`run_py entry_equiv.py judge --a-dir …/a --b-dir …/b --expect-steps 100`（不传 `--expect-sha256`）；
   - wandb 探针（独立小步，不进训练进程）：`WANDB_PROJECT` 设置时 `run_py - <<'EOF' import wandb; init(project/entity/name='gl-wandb-probe', mode='online'); log; finish EOF`，判定行 `WANDB_PROBE=PASS`；
   - 四段 RC（A/B/JUDGE/PROBE）各自 echo，最终首个非零，trap 写 `EXIT_CODE=`。
5. **worktree 重建**：`git worktree add --detach v1-store/entryeq/worktree-main ecf086c…` + `cd … && UV_LINK_MODE=copy uv sync`（本机执行，venv 落 turbo、集群 `--no-sync` 直用；与 C4 同法）。
6. **探针 run 清理**：`wandb.Api().run("hongzefu-university-of-michigan/robomme-framesamp/<probe_id>").delete()`（本机已产生 1 个 `wandb-probe-delete-me`）。
7. **秒级闸**：`ruff check` 改动文件零新增；`uv run pytest --collect-only -q src scripts packages` 零 error；`bash -n` 两个 sbatch；`WANDB_PROJECT` 未设路径的 sbatch 行为等价人工 diff 复核。
8. **commit `commitV5.1`**（逐文件 add：train.py、entry_equiv.py、gl_train_prod.sbatch、gl_entry_eq.sbatch）。

## D2 runbook（v1-gl-entry-eq）

- 前置：clean HEAD、run_name 确认（AGENTS 6）、launch.md 先行提交、worktree 与两库在 turbo 就位、用户放行（超调试包络：4 GPU/3h）+ 询问 Okta 方式，经 `scripts/train-hongzefu/gl_submit.py` 提交（`GLPW`/`GLOTP` 规约照 `greatlakes.md`）。
- 判定行：
  ```
  A_SIDE_SEGMENTS tentative_rows=12 main_rows=100
  ENTRY_SCALARS steps=100 keys=5 hex_mismatch=0
  ENTRY_STATE_DIGEST rows=<首轮钉死，预期 4：步 25/50/75/99> mismatch=0
  ENTRY_RESOLVED_CFG mismatch=0 whitelist=4
  ENTRY_PROVENANCE=PASS
  WANDB_PROBE=PASS
  ENTRY_EQ=PASS        # 四项（无锚点子判据）
  ```
- FAIL 处置：`ENTRY_SCALARS` 失配时先同侧重跑一次 B 侧甄别 A40 硬件不确定性（D2 flags 在 A40 首测），仍失配再对比入口；不得直接判「入口改坏」。
- 留档 `docs/training-doc/v1-gl-entry-eq/`（launch/result/records 拷贝 + judge 输出）；PASS 后清理 worktree、`v1-store/entryeq/`、集群侧 jax 缓存软链。

## D3 runbook（v1-prod-60k）

- 提交参数（经 `--export`）：`RUN_NAME=v1-prod-60k STEPS=60000 SAVE_INTERVAL=10000 LOG_INTERVAL=100 BATCH=64 FSDP=4 WORKERS=8 SEED=335(现默认，起跑前与用户确认 seed) WANDB_PROJECT=robomme-framesamp WANDB_ENTITY=hongzefu-university-of-michigan WANDB_API_KEY=<会话内提供>`；`#SBATCH --time=96:00:00`（sbatch 现 12h 需同步改或经提交器覆盖 `--time`）。
- keep_period：具名配置已是 10_000，无需传参。
- 判读：跑完 `util/analyze_gpu_util.py --trend`（sbatch 自动）+ wandb 曲线；留档 `docs/training-doc/v1-prod-60k/`。
- 起跑前逐项：run_name 确认、launch.md 提交、clean HEAD、`RECORD_DIR`/`CKPT_DIR` 不存在、用户放行（4×A40×96h ≈ 384 GPU·h，远超包络）+ Okta。

## 验证方式汇总

- D1：秒级闸四件 + sbatch 无 wandb 路径等价 diff + 本机探针清理确认。
- D2：上表判定行全绿即 D3 放行前提之一；`WANDB_PROBE=PASS` 证计算节点出网。
- D3：起跑后 Monitor 首小时盯 wandb init 成功与步时（应 ≈4.8s）；结束 `TREND_OK` + `EXIT_CODE=0` + wandb 曲线与 metrics.jsonl 交叉核对。

## 在途改动申明

`scripts/training/train.py` 的 entity 一行已在本计划批准前改毕（工作区在途、未提交）。计划获批 → 随 commitV5.1 提交；被否 → `git checkout -- scripts/training/train.py` 回退。
