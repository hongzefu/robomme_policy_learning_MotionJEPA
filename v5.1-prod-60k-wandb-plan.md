# v5.1 计划（第二版）：60k 全量训练提交——wandb 新 project + framesamp-context + 集群侧上游对拍

> **本文件是 v5.1 工作的权威计划**（2026-08-30 第二版，经用户方对抗审计逐条修订后定稿，待批准实施）。
> 第一版（commit `4da7f12`）经审计发现 judge 假阳性孔洞、「位级前缀」表述错误、96h 未落实、
> worktree venv 集群不可用、提交器路径错误等 P1 问题，本版逐条修复；已核对正确的部分
> （80.23h 推算、6 ckpt≈78GB、wandb 调用点结论、D2 单 job 串行方向、RC 聚合）保持不变。
> **锚点**：分支 `v1-dataloader-Restructure`，HEAD = `4da7f12`（工作区 clean；v5.0 四步全绿：
> C2 `f641f40` / C2.5 / C3 G0_EQ=PASS / C4 ENTRY_EQ=PASS）。
> **commit 编号**：代码切片 **commitV5.1**；集群 run 拟名 `v1-gl-entry-eq`（D2）与
> `v1-prod-60k`（D3），起跑前按 AGENTS 6 逐一确认。

# 第一部分（给人看）

## Context

v5.0 已证明新 `train.py` 单跑入口与上游 main `ecf086c` 官方入口本机位级等价（C4，
`docs/training-doc/v1-upstream-eq/`）。现在提交**首次全量正式训练**：60,000 步、
framesamp packed 库 + `perceptual-framesamp-context`、开启 wandb（新 project），并把
与上游的位级对拍延伸到集群 A40 硬件（D2），作为 384 GPU·h 正式 run（D3）的放行闸。

## 用户拍板汇总（2026-08-30，两轮 AskUserQuestion）

| 议题 | 拍板 |
|---|---|
| wandb project | **`robomme-framesamp`**（已由本机探针建立） |
| wandb entity | `WANDB_ENTITY` 环境变量覆盖、默认仍 `daiyp_umich`。依据：实测该 key 写 `daiyp_umich` 403、写 `hongzefu-university-of-michigan` PASS |
| API key | 会话内已提供；只经环境变量（`--export`）传递，不落文件不进 git |
| D2 步数 | **1000 步**（第二轮改定；100 步方案废止——累积灵敏度与 A40 确定性首测暴露窗口不足） |
| D3 seed | **42**（官方顶层默认，弃 sbatch 现默认 335） |
| D3 log_interval | **100**（官方口径；判读降级兜底见 D3 节） |
| D3 workers | **8**（吞吐实测档；batch 组成与顺序由 seed 决定，与官方 4 数值等价） |
| ckpt | 官方口径 save 10000 / keep 10000（keep 由具名配置自带，6 ckpt ≈ 78 GB，单 ckpt 实测 13 GB） |
| 步数落点 | `STEPS=60000` 走提交时 `--export` 覆盖，全局默认不动（AGENTS 10） |

## 关键结论一：wandb 记录与 main branch 一致（已核实，保持第一版结论）

`init_wandb` 与 `wandb.log` 调用点与上游逐字相同（唯一差异 `log_code` 路径修正且默认不触发）；
step 0 一次 `camera_views` + 每 `log_interval=100` 步一次五标量均值。entity 的 env 覆盖只改
上报归属不改内容。**60k 的 wandb 曲线口径与上游官方训练完全同构。**

## 关键结论二（第二版修正）：60k 的「前缀」是语义前缀、不是官方 run 的位级前缀

- 具名配置 lr：warmup 10k / peak 5e-5 / decay_steps 100k / **decay_lr 5e-5 == peak_lr**
  （`src/mme_vla_suite/training/config.py` 具名配置块）——实际是 **warmup 10k 后恒定 5e-5**，
  不发生 cosine 下降；该 schedule 不依赖 `num_train_steps`。
- 正确主张：**若 D3 其余参数完全相同、仅把 `num_train_steps` 从 60k 延长到 80k，则前
  60k 个更新构成语义前缀**。与「官方默认参数的 80k run」无逐位关系（本 run 数据链是
  packed+context，与官方 legacy 示例参数集不同；第一版「官方 80k 位级前缀」表述作废）。

## 实测底座与动态状态

- 速率：`v1-prod-trend-10h` 实测 4×A40/b64 步时均值 4.814 s → 60k ≈ **80.23 h**，walltime
  **96 h**（1.20×）。12h 只能跑约 8,974 步——**96h 必须落在提交命令里**（见 D3 钉死命令）。
- 磁盘/分区上限/wandb 可用性属动态外部状态：**D3 提交前重新实测留档**（`df`、
  `scontrol show partition spgpu` 查 MaxTime、wandb 探针复跑）。本轮参考值：turbo 剩 6 TB。

## 三步走

**D1（本机代码，commitV5.1）**：`train.py` entity 一行（**待修改**，第一版「在途」表述作废
——该编辑已随 plan mode 回退，当前工作区 clean）；`entry_equiv.py` judge 全面硬化（堵审计
列出的全部假阳性孔洞）；`gl_train_prod.sbatch` wandb 条件块 + 四件闭环（起跑 HEAD/porcelain
断言、ENV_RC 立即 abort、train 输出 tee 副本、WANDB_MODE 强制 online）；新增
`gl_entry_eq.sbatch`；worktree venv 按 greatlakes.md NFS 解释器硬规则重建；验证升级为
「真实执行」而非纯静态闸。

**D2（集群 job `v1-gl-entry-eq`，提交前放行 + Okta 流程）**：单 job 4×A40/16C/128G/**5h**，
串行 A（上游 `ecf086c`，tentative 12 步 + 正式 **1000 步**，`--overwrite`，legacy 库）与
B（新入口，1000 步，packed 库），生产口径 b64/fsdp4 + 确定性档，judge 硬判（详见第二部分
判定行）→ `ENTRY_EQ=PASS`；同 job 真代码 wandb 探针（跑完即删 probe run）。⚠ 风险：确定性
XLA 档 A40 首测；`ENTRY_SCALARS` 失配先同侧重跑甄别硬件不确定性，再谈入口差异。
留档 `docs/training-doc/v1-gl-entry-eq/`（AGENTS 17）。

**D3（集群 job `v1-prod-60k`）**：完整提交命令钉死于第二部分（`--time=96:00:00`、
`--job-name=v1-prod-60k`、逐项 `--export` 含 `SEED=42 LOG_INTERVAL=100 WORKERS=8
STEPS=60000 SAVE_INTERVAL=10000 WANDB_*` 与 `EXPECTED_GIT_HEAD`）。wandb 真实链路
（`init_wandb→_MetricsProxy`）首验就在 D3 起跑首分钟：`wandb.init` 位于权重加载与 JIT 之前，
失败分钟级即死、Monitor 盯 init 成功行，损失可控。判读：`TREND_OK`（util 均值 + 前后半段
趋势 + 0% 占比）+ dense 500ms 通道分段趋势；log100 下慢步/非慢步分层不可算（AGENTS 16 的
该分项以 prod-trend 同款降级口径留档说明，用户已拍板接受此取舍）。

---

# 第二部分（技术细节，供 agent 追踪）

## D1 逐处改动

1. **`scripts/training/train.py`**（待修改）：`init_wandb` 内 `entity="daiyp_umich"` →
   `entity=os.environ.get("WANDB_ENTITY", "daiyp_umich")`，注释写明 403 实测依据。
2. **`scripts/training/tests/entry_equiv.py`**：
   - run 侧：provenance.json 增记 `git rev-parse HEAD` 与 `git status --porcelain` 输出
     （cwd 侧）；新增可重复参数 `--forbid-root <dir>`——断言所有项目模块 `__file__`
     **不在**该目录下（B 侧传 worktree 路径；A worktree 是 `$REPO/v1-store/` 子目录，
     仅 `is_relative_to(expect_root)` 拦不住 B 误载 A 模块）。
   - judge 侧新增/硬化：`--expect-steps N`（scalar 步集合必须**恰为** `0..N-1`，两侧同）；
     `--expect-tentative-a 12`（A 段恰 `12/N`）与 B 段恰 `0/N`，**进退出码**；
     `--expect-state-steps 100,200,...,900,999`（状态摘要步集合恰为此集、原始行数 == 集合
     大小、无重复，两侧同）；`--expect-head-a ecf086c… --expect-head-b <sha>`（比对
     provenance 记录的 HEAD）+ 两侧 porcelain 必须为空；`--expect-sha256` 改可选（缺省打
     `anchor=SKIPPED`，`ENTRY_EQ` = 其余全部硬判项）。
3. **`scripts/training/prod/gl_train_prod.sbatch`** 四件闭环 + wandb 条件块：
   ```bash
   # 起跑闸（write_env 之前）：排队期间仓库变动 fail-fast
   if [ -n "${EXPECTED_GIT_HEAD:-}" ]; then
       [ "$(git -C "$REPO" rev-parse HEAD)" = "$EXPECTED_GIT_HEAD" ] || { echo "错误: HEAD 漂移"; exit 1; }
       [ -z "$(git -C "$REPO" status --porcelain)" ] || { echo "错误: 工作区不 clean"; exit 1; }
   fi
   # wandb 条件块（未设 WANDB_PROJECT 时与现状逐字节等价）
   if [ -n "${WANDB_PROJECT:-}" ]; then
       [ -n "${WANDB_API_KEY:-}" ] || { echo "错误: 缺 WANDB_API_KEY"; exit 1; }
       export WANDB_MODE=online          # 强制，不允许继承 disabled/offline
       export WANDB_DIR="$STORE/cache/wandb" WANDB_CACHE_DIR="$STORE/cache/wandb" WANDB_CONFIG_DIR="$STORE/cache/wandb"
       mkdir -p "$WANDB_DIR"; WANDB_ARGS=(--project-name "$WANDB_PROJECT")
   else
       export WANDB_MODE=disabled; WANDB_ARGS=(--no-wandb-enabled)
   fi
   ```
   另：`write_env` 后 `ENV_RC` 非零**立即 `exit "$ENV_RC"`**（不再带病跑 80h）；训练命令改
   `... 2>&1 | tee "$RECORD_DIR/train_console.log"`（`TRAIN_RC=${PIPESTATUS[0]}`）；训练
   argv 尾部 `--no-wandb-enabled` → `"${WANDB_ARGS[@]}"`；`WANDB_ENTITY` 经 export 透传。
   条件块抽成函数 `resolve_wandb_args`，供本机双分支实测（见 D1 验证）。
4. **新增 `scripts/training/tests/gl_entry_eq.sbatch`**：
   - `#SBATCH`：`--account=chaijy2 --partition=spgpu --nodes=1 --gpus-per-node=4
     --cpus-per-task=16 --mem=128G --time=05:00:00 --output=$REPO/v1-store/logs/%x-%j.log`；
   - 起跑闸同上（`EXPECTED_GIT_HEAD` 必传）+ worktree 侧断言 `git -C $WORKTREE rev-parse
     HEAD == ecf086c…` 且 porcelain 空 + **venv 复核**：`readlink -f $WORKTREE/.venv/bin/python`
     必须以 `/nfs/turbo/` 开头；
   - 共同 env：`XLA_FLAGS='--xla_gpu_deterministic_ops=true --xla_gpu_autotune_level=0'`、
     `XLA_PYTHON_CLIENT_MEM_FRACTION=0.95`、jax 缓存软链 `~/.cache/jax_gl-entry-eq-{a,b}`
     → `$STORE/cache/jax/`（照抄 prod sbatch 惯例）；
   - A 侧：`cd $WORKTREE && env -u PYTHONPATH -u PYTHONHOME uv run --no-sync --project
     "$WORKTREE" python $REPO/scripts/training/tests/entry_equiv.py run --entry
     $WORKTREE/scripts/train.py --record-dir $STORE/entryeq/records/a --expect-root
     $WORKTREE --expect-steps 1000 -- mme_vla_suite --exp-name gl-entry-eq-a --overwrite
     --num-train-steps 1000 --log-interval 1 --save-interval 100 --batch-size 64
     --num-workers 8 --seed 42 --fsdp-devices 4 --dataset-path $STORE/datasets/4task-gl
     --assets-base-dir $STORE/train-assets --checkpoint-base-dir $STORE/entryeq/ckpt-a
     --weight-loader.params-path $STORE/models/openpi-assets/checkpoints/pi05_base/params
     --model.use-history --model.history-config perceptual-framesamp-context.yaml
     --no-wandb-enabled`；
   - B 侧：入口 `$REPO/scripts/training/train.py`、`--exp-name gl-entry-eq-b`、无
     `--overwrite`、packed 库、`ckpt-b`、`--expect-root $REPO`、**`--forbid-root $WORKTREE`**、
     `env -u TRAIN_RECORD_DIR`，其余逐字符同（两侧均 `--no-wandb-enabled`，对拍纯净）；
   - judge：`run_py entry_equiv.py judge --a-dir …/a --b-dir …/b --expect-steps 1000
     --expect-tentative-a 12 --expect-state-steps 100,200,300,400,500,600,700,800,900,999
     --expect-head-a ecf086c3be7c2223167d9bb2f6ef1f0a6e24353b --expect-head-b
     $EXPECTED_GIT_HEAD`（不传 `--expect-sha256`——A40 无本机锚点）；
   - wandb 探针（可执行真代码，heredoc 进 `run_py -`）：`wandb.init(project=$WANDB_PROJECT,
     entity=$WANDB_ENTITY, name="gl-wandb-probe", mode="online")` → log 一点 → finish →
     `wandb.Api().run(f"{entity}/{project}/{run.id}").delete()`，判定行 `WANDB_PROBE=PASS`；
   - 五段 RC（A/B/JUDGE/PROBE/ENV）各自 echo、最终首个非零、trap 写 `EXIT_CODE=`。
5. **worktree 重建（greatlakes.md venv 硬规则）**：
   ```bash
   git worktree add --detach v1-store/entryeq/worktree-main ecf086c3be7c2223167d9bb2f6ef1f0a6e24353b
   cd v1-store/entryeq/worktree-main
   uv venv --python /nfs/turbo/coe-chaijy-unreplicated/hongzefu/uv-python/cpython-3.11.14-linux-x86_64-gnu/bin/python3.11
   UV_LINK_MODE=copy uv sync --frozen
   readlink -f .venv/bin/python   # 必须以 /nfs/turbo/ 开头
   ```
6. **D1 验证（真实执行，不止静态闸）**：
   - 秒级闸：ruff 零新增、`pytest --collect-only` 零 error、`bash -n` 两 sbatch；
   - judge 新分支实跑：用 C4 固化 records（`docs/training-doc/v1-upstream-eq/records/{a,b}`）
     跑 `judge --expect-steps 1000 --expect-tentative-a 12 --expect-state-steps 100,…,999`
     两分支（带锚点必 PASS、不带锚点必 PASS 且打 SKIPPED）+ 负样本（/tmp 拷贝上删状态摘要
     一行 / 改 HEAD 期望值 / 伪造 tentative 行数）必 FAIL；
   - sbatch wandb 条件块：`resolve_wandb_args` 本机 source 实跑两分支，断言 argv 与 env；
   - 本机 wandb 探针 run 清理（既有 `wandb-probe-delete-me` 经 API delete）。
7. **commit `commitV5.1`**（逐文件 add：train.py、entry_equiv.py、gl_train_prod.sbatch、
   gl_entry_eq.sbatch）。

## D2 runbook（v1-gl-entry-eq）

- 前置：clean HEAD、run_name 确认（AGENTS 6）、launch.md 先行提交、worktree venv 复核、
  用户放行（4×A40×5h ≈ 20 GPU·h，超调试包络）。
- 提交（提交器唯一正确路径 `scripts/dataset/gl/gl_submit.py`；先 `ssh -O check greatlakes`，
  exit 0 则零认证直接提交，非 0 才问用户认证方式建 master）：
  ```bash
  uv run --no-project --with pexpect python scripts/dataset/gl/gl_submit.py \
    "cd $REPO && sbatch --job-name=v1-gl-entry-eq \
       --export=ALL,EXPECTED_GIT_HEAD=<提交时 HEAD>,WANDB_PROJECT=robomme-framesamp,WANDB_ENTITY=hongzefu-university-of-michigan,WANDB_API_KEY=<key> \
       scripts/training/tests/gl_entry_eq.sbatch"
  ```
- 判定行：
  ```
  A_SIDE_SEGMENTS tentative_rows=12 main_rows=1000     # 硬判进退出码
  B_SIDE_SEGMENTS tentative_rows=0 main_rows=1000      # 硬判进退出码
  ENTRY_SCALARS steps=1000 keys=5 hex_mismatch=0       # 步集合恰 0..999
  ENTRY_STATE_DIGEST rows=10 mismatch=0                # 步集合恰 {100..900,999}，无重复
  ENTRY_RESOLVED_CFG mismatch=0 whitelist=4
  ENTRY_PROVENANCE=PASS                                # 含 HEAD 断言 + porcelain 空 + forbid-root
  WANDB_PROBE=PASS
  ENTRY_EQ=PASS
  ```
- FAIL 处置：`ENTRY_SCALARS` 失配 → 先同侧（B）重跑一次甄别 A40 确定性档硬件问题，仍失配
  再查入口；不得直接判「入口改坏」。
- 留档 `docs/training-doc/v1-gl-entry-eq/`；PASS 后清理 worktree、`v1-store/entryeq/`、
  集群侧 `~/.cache/jax_gl-entry-eq-*` 软链。

## D3 runbook（v1-prod-60k）——提交命令钉死

- 提交前 preflight（全部重新实测并写入 launch.md）：`df -h` turbo 剩余（需 ≥ 200 GB 裕度）；
  `ssh greatlakes 'scontrol show partition spgpu | grep -o "MaxTime=[^ ]*"'`（须 ≥ 96h，
  否则回用户重议）；本机 wandb 探针复跑（key/entity/project 仍有效）；clean HEAD；
  run_name 确认；launch.md 先行提交；用户放行（4×A40×96h ≈ 384 GPU·h）+ Okta 流程
  （`ssh -O check` 优先）。
- 提交命令（`--time`/`--job-name` CLI 覆盖 sbatch 头部指令；`SEED=42` 覆盖脚本默认 335）：
  ```bash
  uv run --no-project --with pexpect python scripts/dataset/gl/gl_submit.py \
    "cd $REPO && sbatch --job-name=v1-prod-60k --time=96:00:00 \
       --export=ALL,RUN_NAME=v1-prod-60k,STEPS=60000,SAVE_INTERVAL=10000,LOG_INTERVAL=100,BATCH=64,FSDP=4,WORKERS=8,SEED=42,EPOCH_STEPS=6176,EXPECTED_GIT_HEAD=<提交时 HEAD>,WANDB_PROJECT=robomme-framesamp,WANDB_ENTITY=hongzefu-university-of-michigan,WANDB_API_KEY=<key> \
       scripts/training/prod/gl_train_prod.sbatch"
  ```
  keep_period 由具名配置自带 10_000，无需传参。
- 起跑首小时 Monitor（远程日志经 gl_submit 拉取或 NFS 侧 tail）：`wandb.init` 成功行
  （真实 `init_wandb→_MetricsProxy` 链路首验，失败分钟级即死）+ 首步步时（应 ≈4.8s）+
  `ENV_RC=0`。
- 判读与留档：`TREND_OK` + dense 分段趋势 + wandb 曲线与 `metrics.jsonl` 交叉核对；
  log100 慢步分层缺失以 prod-trend 同款降级口径写明；留档 `docs/training-doc/v1-prod-60k/`。

## 验证方式汇总

- D1：上述「真实执行」清单全绿后 commit。
- D2：判定行全绿 = D3 放行前提；`WANDB_PROBE=PASS` 证计算节点出网。
- D3：首小时 Monitor 三项 + 结束 `TREND_OK`/`EXIT_CODE=0`/wandb-metrics 交叉核对。
