# v5.0 统一入口计划：`scripts/training/main.py` 单跑薄封装 + main 分支对拍等效性验证

> **本文件是统一入口工作的唯一权威文档**（2026-08-30 定稿）。前身是 `v3-destructive-restructure-plan.md` 第三部分（commitV3.15，2026-08-29 批准的「双跑照抄 + 同分支对拍」方案），该部分已缩为指针、以本文件为准。本轮按用户 2026-08-30 拍板改为**单跑薄封装 + main 分支对拍**，编号改定 **commitV5.0**（原批准编号 V3.15；因 V4.x 序列已落地、避免 git log 编号倒序，用户拍板「改为从5.0开始」）。
>
> **锚点**：分支 `v1-dataloader-Restructure`，定稿时 HEAD `0eb69c127ef0738e614cedc8c88ae02498061605`（G3 收官 docs commit），工作区 clean。对拍基准侧：`main` 分支 tip `ecf086c3be7c2223167d9bb2f6ef1f0a6e24353b`（该 commit 同时是本分支的分叉点——分支落后 main 零 commit，实测 `git merge-base` 与 main tip 同值）。
>
> **红线**：`scripts/training/train.py`、`scripts/training/bench/`、`scripts/training/prod/` 三处**零字节改动**；G 链量具、G0b 引用、测速口径原封不动。等效性以位级判据机器裁决，不接受口头声明。

---

# 第一部分（给人看）

## 一、Context 与用户拍板轨迹

v3/v4 系列收官后（G3 四分项全绿，scalars sha256 与 G0b/G1/G2 第五份同值），训练的用户入口仍是分散的六个脚本。本轮新增单一用户入口 `scripts/training/main.py`，并证明它与**最初的 main 分支官方启动路径**训练数值位级等价。

用户需求原话（按时间序，技术名词保留原文；原话中的 `scripts/train.py` 实际为 `scripts/training/train.py`——commitV4.6 目录统一后的路径，官方 shell `finetune_mme_vla_suite.sh` 的启动命令行实写 `uv run scripts/training/train.py mme_vla_suite`，本计划一律用真实路径）：

- 2026-08-29：「我希望……修改后 尽可能调用一个python文件」→「不！我要的是一个类似scripts/train.py的统一接口」→「需要保证改为统一接口后 测速 正确对拍 和原版能对齐 并且增加一个等效性测试 测试和现有的 scripts/train.py 等效 训练数值上等价」（当日以 commitV3.15 之名批准写入 v3 计划第三部分）。
- 2026-08-30：「是否可以只保留一个 train.py 单次训练而不是两次训练的接口 然后再做和最早的https://github.com/hongzefu/robomme_policy_learning_MotionJEPA/tree/main 的对拍测试 作为入口的等效性？」
- 2026-08-30：「总结一下 改为薄封装后 需要和main 分支对齐 同时g0的对齐在bench/中也能正常工作 测速也要和以前行为一致」
- 2026-08-30 逐项拍板：commit 编号「改为从5.0开始」；旧第三部分「缩为指针」；「写计划」「计划单独的md在根目录」。

## 二、做什么

新增单一用户入口 `scripts/training/main.py`：第一个位置参数选子命令，其余参数交给该子命令自己现有的 CLI，不做任何参数改写：

```bash
uv run scripts/training/main.py train mme_vla_suite --exp-name=... --batch-size=64 \
    --model.use_history --model.history_config=perceptual-framesamp-context.yaml ...
uv run scripts/training/main.py serve ...        # → serve_policy.py（本就是 tyro）
uv run scripts/training/main.py norm-stats ...   # → compute_norm_stats.py（本就是 tyro）
uv run scripts/training/main.py results ...      # → compute_results.py（argparse 透传）
uv run scripts/training/main.py unzip-ckpt ...   # → unzip_ckpt.py（argparse 透传）
uv run scripts/training/main.py download-base    # → download_pi05_base.py
```

**`train` 子命令是单跑薄封装**：`_config.cli()` 解析一次、`train.main(config)` 只调一次，**不带 `tentative_run`**——与集群正式入口 `scripts/training/prod/prod_train_once.py` 同语义。`train.py` 尾部带双跑的 `__main__` 块原样留置作历史，不再是官方入口。config 名仍是二级子命令（`_config.cli()` 即 `tyro.extras.overridable_config_cli`），`main.py train mme_vla_suite --exp-name=x` 与脚本模式 `train.py mme_vla_suite --exp-name=x` 逐字符同参。G 链量具（bench 对拍/测速那套）按既有纪律不收进子命令，继续独立。

### 目录组织前后对比（用户已批准）

修改前（现 HEAD，略 `__pycache__`）：`scripts/training/` 下 10 个顶层文件（`train.py`、`serve_policy.py`、`compute_norm_stats.py`、`compute_results.py`、`unzip_ckpt.py`、`download_pi05_base.py`、`finetune_mme_vla_suite.sh`、`eval.sh`、`paths.sh`、`__init__.py`）+ `bench/`（7 文件）+ `prod/`（3 文件）+ `tests/`（10 文件）。

修改后——**新增且仅新增 4 个文件，其余零字节不动**：

```
scripts/training/
├── main.py                     # ★新增：统一入口分发器（单跑薄封装；与 train.py 同目录，
│                               #   这是 sys.path[0] 差异归零论证的前提，不能挪）
├── train.py                    # 冻结（TRAIN_PY_FROZEN 闸）
├── （其余 9 个顶层文件不动）
├── bench/                      # 全目录冻结（同闸；量具不收进子命令）
├── prod/                       # 全目录冻结（同闸）
└── tests/
    ├── test_entry_config_eq.py # ★新增：ENTRY_CONFIG_EQ 秒级 pytest
    ├── entry_equiv.py          # ★新增：ENTRY_MAIN_EQ 单侧 harness（观测补丁 + runpy）
    ├── run_entry_equiv.sh      # ★新增：ENTRY_MAIN_EQ 驱动（两侧 subprocess + 位级比对 + 判定行）
    └── （既有 10 个文件不动）
```

`scripts/training/` 之外的落点：本计划文件（仓库根）；执行期留档 `docs/training-doc/v1-entry-eq/`；A 侧临时 worktree 与两侧临时产物收敛在 `v1-store/entryeq/` 下。

## 三、为什么单跑成立（关键决策依据）

**登记阻断**：`train.py` `__main__` 的双跑结构（tentative 预热 → `time.sleep(20)` → 正式跑）**照现状必崩**——第一次调用里 `initialize_checkpoint_dir`（`src/openpi/training/checkpoints.py`）已把 checkpoint 目录建出来，第二次调用在默认 `overwrite=False / resume=False` 下走进该函数的 `FileExistsError` 分支；官方 shell `finetune_mme_vla_suite.sh` 不带 `--overwrite`。此阻断在 `prod_train_once.py` 文件头登记为「正式入口的可运行性阻断」，v1 全部 e2e 走 bench 入口、集群走 prod 单跑入口，所以从没撞上。

**tentative 零数值影响**：tentative 那次是独立初始化、跑约 12 个 optimizer step 后状态全部丢弃（`train.py` 内 `tentative_run_step = 10`，越过即 break），目的只是集群 A40 预热。依据：`scripts/training/bench/README.md` 差异表 B4 条（「tentative 那次独立初始化、状态全部丢弃，本来就不影响正式轨迹」）、`prod_train_once.py` 文件头论证、以及 prod 单跑入口跑通 10h 正式 run 的实证。

**结论**：单跑薄封装 = 官方双跑语义的正式段 = prod 生产语义。它顺带消掉了 `FileExistsError` 议题——B 侧（新入口）不需要 `--overwrite`；只有 ENTRY_MAIN_EQ 的 A 侧（main 分支官方双跑）需要加 `--overwrite` 才能跑完正式段。

## 四、三条硬保证（对应用户三条要求）

### 保证一：与 main 分支对齐——ENTRY_MAIN_EQ（真跑 main 分支官方入口做位级对拍）

| | A 侧（原版基准） | B 侧（统一接口） |
|---|---|---|
| 代码 | main tip `ecf086c3be7c` 的 git worktree | 本分支 V5.0 tip |
| 入口 | `scripts/train.py` 官方 `__main__` 双跑（runpy 以 `__main__` 身份执行），**加 `--overwrite`** | `scripts/training/main.py train` 单跑 |
| 数据 | legacy 库 `v1-store/datasets/4task-gl` | packed 库 `v1-store/datasets/4task-gl-framesamp` |
| venv | worktree 内独立 `uv sync`（同一份 `uv.lock`） | 仓库现行 venv |
| 数值口径 | 2 卡 / b8 / 20 步 / seed 42 / workers 4 / fsdp 2 / `perceptual-framesamp-context` / 确定性档，两侧逐项相同 | 同左 |
| 判据 | A 正式段 vs B：逐步五标量（loss / grad_norm / llm_grad_norm / mem_enc_norm / param_norm）`float.hex()` 逐位相等 + 末步完整 TrainState（params + Adam 动量 + EMA）逐叶 sha256 相等 | 同左 |

**等价链一行总结**：官方 shell（= env + argv 薄壳）≡ 脚本身份的 `train.py` `__main__` ≡ A 侧；A 正式段与 B 位级判据全 PASS ⇒ `main.py train` ≡ main 分支官方启动路径的正式训练段。

四层依据，逐层可核对：

1. **环境同一性（实测）**：`git diff ecf086c..HEAD -- uv.lock` 为空——分叉至今 `uv.lock` 零 diff；`pyproject.toml` 仅 +9 行且全是 ruff lint 豁免配置（`[tool.ruff.lint.per-file-ignores]` 一节），无任何依赖变更。两侧 jax/XLA 版本严格同一。
2. **跨链位级可达（既有实证，不是新赌注）**：G1 vs G0b 已证跨 dtype、跨计算图下五标量 bitwise 成立（`docs/v1-phase2-dtype-unify-report.md`：「bitwise 全过」，量化兜底未启用）；D2-cold 已证确定性档下独立冷编译两次 bitwise 成立。A/B 计算图不同（legacy vs packed 链）不构成位级判据的障碍。
3. **零成本旁证（G0b 固化前缀）**：B 侧前 20 步 scalars 另与 G0b 固化产物（`docs/training-doc/v1-grad-baseline-g0b/records/`，git 内，`scalars_hex.tsv` sha256 前缀 `c799a0b2…`）位级对拍，前置 `check_baseline_env.py check` 环境指纹 preflight（AGENTS 18 基线复用条款）。G0b 即「三计划均未实施的原始训练语义」两轮千步自证的黄金基线；此旁证把 B 侧同时钉在既有 G 链锚点上。A 侧真跑还顺带补上 `ecf086c`（main tip）↔ `570287f`（G0b 所在 commit）之间从未实测过的最后一小段——此前只凭「量具 commit 不改训练码」的代码审查级论断。
4. **口径差如实标注**：ENTRY_MAIN_EQ 不复刻生产规模（b64 / 4 卡 / 80k 步），用 G 链一贯的 2 卡 / b8 / 20 步确定性档。成立理由：被测变量是「入口方式 + 两条链的同一性」，与规模无关，且两侧规模严格相同；「不同 config 组合下 argv 都被正确交付」由 ENTRY_CONFIG_EQ 单独闸住。A 侧 tentative 段（预期 step 0..11 共 12 行，以首轮实测钉死）B 侧没有——单独留档、不进判据，依据三节「tentative 零数值影响」。

### 保证二：bench/ 的 G0 对齐照常工作——构造性成立 + 机器闸 + 实测复核

G 链测速与对拍从来不走任何入口：`bench_train_steps.py` 是 `import train` 后直调 `train.main(config)`（其文件头自述「训练循环一行不改」），四个源码指纹护栏锚在 `train.main` 函数体上。本轮 `train.py`、`bench/`、`prod/` 零字节改动（判定行 `TRAIN_PY_FROZEN`），`main.py` 不被任何训练链文件 import（判定行 `ENTRY_ISOLATION`）——G0b 引用、`compare_baseline.py` 判据、环境指纹 preflight 一概不受扰。另跑一轮 STEPS=5 确定性烟测（bench 原路）vs G0b，`SCALARS steps=5 hex_mismatch_steps=0` 作实测复核。

### 保证三：测速行为与以前一致——构造性成立

speed 链整条路径（`run_2gpu_epoch_bench.sh` → `bench_train_steps.py` → `train.main`）与 prod 集群链全部在冻结面内，字节不变即行为不变，无需重测。`main.py` 是纯增量文件，经 `ENTRY_ISOLATION` 保证不进这两条路径。

## 五、argv 管道与入口方式差异归零

**ENTRY_CONFIG_EQ（秒级 pytest）**：`main.py train` 分支把 `sys.argv` 改写为 `[argv0, *rest]` 后调 `_config.cli()`——与脚本模式同构。pytest 对代表性 argv 矩阵（三个 framesamp 变体 × exp-name / batch-size / num-workers / model.* override 组合）跑 `main.py` 分发捕获 config，与直接置 `sys.argv` 调 `_config.cli()`（脚本模式语义）的对照 config 做 `dataclasses.asdict` 逐键比对。判定行 `ENTRY_CONFIG_EQ=PASS cases=<N> mismatch=0`。

**「直接跑一个 .py」vs「被 main.py 分发」的差异清单，逐条归零**（单跑薄封装下结论不变）：

| 差异点 | 脚本模式 | main.py 模式 | 影响 |
|---|---|---|---|
| `sys.path[0]` | `train.py` 所在目录 | `main.py` 所在目录 | 零：两文件同在 `scripts/training/`，字面相同；且 `train.py` 的 import 全是 `openpi.*`/`mme_vla_suite.*` 已装包，不依赖 path[0] |
| `__name__` | `"__main__"` | `"train"` | 零：`train.py` 全文 `__name__` 只出现在入口守卫处，无其他依赖 |
| `__file__` | `train.py` 自身路径 | 仍是 `train.py` 自身路径 | 零：`wandb.run.log_code(epath.Path(__file__).parent.parent)` 绑的是 train 模块的 `__file__`，import 方式不改变它 |
| spawn worker 重导入 `__main__` | `__main__`=train.py | `__main__`=main.py | 零，但有一条纪律：进 worker 的对象全定义在 `mme_vla_suite.*` 包里、按模块名 pickle；`main.py` 必带 `if __name__ == "__main__":` 守卫（风险登记 R1） |
| `argv[0]` | `scripts/training/train.py` | `scripts/training/main.py` | 仅进 tyro `--help` 的 prog 名，纯外观，不进解析 |

模块级副作用相同：`train.py` 模块体只有 import，两种入口都恰好执行一次、时机都在环境变量已就位之后（`main.py` 分发阶段零重依赖 import，各分支惰性 import，jax/XLA 环境变量仍以命令前缀生效）。

## 六、入口链路前后图（AGENTS 18 映射）

本轮不触碰数据链任何一跳（交付形状/dtype/字节零变化，逐跳图引用 G3 留档 `docs/training-doc/` 既有链路图，不重画）；变的只有「入口跳」：

```
【前：官方入口（登记阻断，实际不可运行）】
finetune_mme_vla_suite.sh（env + argv 薄壳）
  → uv run scripts/training/train.py mme_vla_suite ...
  → __main__: train.main(cli(), tentative_run=True)   # 预热段，建出 ckpt 目录
  →           time.sleep(20)
  →           train.main(cli())                        # ✗ FileExistsError（无 --overwrite 必崩）
（实际生产绕行：bench → import train 直调；集群 → prod_train_once.py 单跑）

【后：统一入口（单跑）】
uv run scripts/training/main.py train mme_vla_suite ...
  → main.py train 分支：sys.argv = [argv0, *rest]
  → config = _config.cli()          # 与脚本模式逐字符同参（ENTRY_CONFIG_EQ 闸）
  → train.main(config)              # 只调一次，无 tentative（≡ prod 语义）
（bench / prod 两条既有路径零变化，不经 main.py）
```

AGENTS 18 两块纪律的映射：第一块（非训练轻量化测试）= ENTRY_CONFIG_EQ；第二块（真实训练数值一致，最后检验）= ENTRY_MAIN_EQ + G0b 前缀旁证。第二块不通过不得宣称等价。

## 七、判定行汇总（commit 前全绿）

```
TRAIN_PY_FROZEN=PASS        # git diff 0eb69c1..HEAD -- scripts/training/train.py scripts/training/bench scripts/training/prod 为空
ENTRY_ISOLATION=PASS        # grep -rnE '^\s*(import main\b|from main import)' scripts src 为空
ENTRY_CONFIG_EQ=PASS cases=<N> mismatch=0
A_SIDE_SEGMENTS tentative_rows=<实测，预期12> main_rows=20     # 留档行，非判据
ENTRY_SCALARS steps=20 keys=5 hex_mismatch=0                  # A 正式段 vs B
ENTRY_STATE_DIGEST rows=1 mismatch=0                          # A 正式段末步 vs B 末步 TrainState 逐叶 sha
G0B_PREFIX=PASS steps=20 keys=5 hex_mismatch=0                # B vs G0b 固化前缀（前置 BASELINE_ENV=PASS）
ENTRY_EQ=PASS               # 以上判据行全绿的总闸
SCALARS steps=5 hex_mismatch_steps=0                          # STEPS=5 烟测 vs G0b（bench 原路，复核量具链）
```

另：`uv run pytest --collect-only -q src scripts packages` 零 error；ruff 零新增违例。

---

# 第二部分（技术细节，供 agent 追踪）

## 八、新增文件与要点

| 文件 | 作用 |
|---|---|
| `scripts/training/main.py` | 统一入口分发器（惰性 import + `__main__` 守卫） |
| `scripts/training/tests/test_entry_config_eq.py` | ENTRY_CONFIG_EQ pytest |
| `scripts/training/tests/entry_equiv.py` | ENTRY_MAIN_EQ 单侧 harness（观测补丁 + runpy） |
| `scripts/training/tests/run_entry_equiv.sh` | ENTRY_MAIN_EQ 驱动（两侧 subprocess + 比对 + 判定行） |

**main.py 要点**：

- 分发阶段零重依赖 import；各分支惰性 import。
- `train` 分支：`sys.argv = [sys.argv[0], *rest]` → `config = _config.cli()` → `train.main(config)`，**单跑、无 tentative、无 sleep**。
- `serve`：`serve_policy.main(tyro.cli(serve_policy.Args, args=rest))`；`norm-stats`：`tyro.cli(compute_norm_stats.main, args=rest)`；`results` / `unzip-ckpt` / `download-base`：把 rest 交给原 parser、不做 argparse→tyro 改写（改写另行拍板）。
- 顶部注释声明「只作入口、禁止被 import」；必带 `if __name__ == "__main__": main()` 守卫。

**entry_equiv.py（单侧 harness）要点**——两处只读观测补丁都在**启动入口之前**装好，A/B 一视同仁：

- 调用形态：`uv run python <harness路径> --entry <入口py路径> --record-dir <dir> -- <入口 argv 尾巴>`；harness 先装补丁，再 `sys.path.insert(0, <入口所在目录>)`、`sys.argv = [<入口名>, *tail]`、`runpy.run_path(<入口>, run_name="__main__")`。A 侧在 worktree venv 内运行（`cd <worktree> && uv run python <真仓库的 harness 绝对路径> ...`），harness 只 import 标准库 + `wandb` + `openpi.training.checkpoints`（各自 venv 解析，A 侧解析到 worktree 的 editable 装载），**不得 import 本分支任何训练代码**。
- **wandb 补丁（登记坑规避）**：不能直接 patch `wandb.log`——`wandb.init()` 会重新赋值模块级 `log` 把补丁盖掉（bench/prod 文件头登记的 2026-08-24 实测坑）。手法：先 `import wandb` 拿到真模块，构造代理对象（显式定义 `log` 方法：按 `float.hex()` 逐步落盘五标量后转发给真 `wandb.log` 的**调用时**查找结果；其余属性 `__getattr__` 委托真模块），`sys.modules["wandb"] = 代理`，runpy 的入口模块 `import wandb` 即绑到代理。记录器按 step 回绕切分 tentative / 正式两段（step 值变小即新段）。
- **save_state 补丁**：`import openpi.training.checkpoints as _cp` 后替换 `_cp.save_state` 为摘要记录器——`train.py` 调用形式是 `_checkpoints.save_state(...)`（调用时模块属性查找），补丁即生效。记录器对完整 TrainState（params + opt_state + EMA + step）逐叶 `sha256(device_get(leaf).tobytes())` 落盘，不写真 checkpoint（无 14 GB 落盘）。预期只在正式段末步触发一次（`--save-interval=10000` > 20 步且末步强制保存），以首轮实测钉死并写进 launch.md。

**run_entry_equiv.sh（驱动）要点**：起 A、B 两个 subprocess（串行即可，同机 2 卡）；跑完读两侧 record jsonl 做比对，输出七节判定行；FAIL 时 record 按 `.failed-<n>` 原子改名保留。

**test_entry_config_eq.py 要点**：经 `importlib.util.spec_from_file_location` 从文件路径加载 `main.py`（不写 `import main`，避免落进 ENTRY_ISOLATION 的 grep pattern）；monkeypatch `train.main` 为捕获桩；对 argv 矩阵跑分发捕获 config，与直接置 `sys.argv` 调 `_config.cli()` 的对照 config 做 `dataclasses.asdict` 逐键比对。矩阵：三个 framesamp 变体 × `--exp-name` / `--batch-size` / `--num-workers` / `--model.*` override 组合。

## 九、ENTRY_MAIN_EQ runbook

**A 侧准备**（一次性）：

```bash
REPO=/nfs/turbo/coe-chaijy-unreplicated/hongzefu/robomme_policy_learning_MotionJEPA
git worktree add --detach $REPO/v1-store/entryeq/worktree-main ecf086c3be7c2223167d9bb2f6ef1f0a6e24353b
cd $REPO/v1-store/entryeq/worktree-main && UV_LINK_MODE=copy uv sync
```

worktree 属临时产物（v1-store 内、不进 git、PASS 后清理），本计划授权在其中执行训练——AGENTS 19 的「快照内禁执行」条款只约束纯审计任务，此处是执行型 worktree，清理责任在发起方（`git worktree remove --force` + `git worktree prune`）。

**preflight（三项，任一不过即停、交用户）**：

- P1 数据兼容冒烟：A 侧先以 `--num-train-steps=2` 短跑（≤5 min），确认 main 代码读 `4task-gl` 出前两步 loss。预期兼容——G0b 即原始语义在该库上跑出，但要实测钉死。
- P2 CLI 键位核对：确认 ecf086c 的 TrainConfig 具备本 runbook 用到的全部旗标（`--overwrite`、`--no-wandb-enabled`、`--num-train-steps` 等；`git show ecf086c:scripts/train.py` 与 config 源码静态核对即可）。
- P3 norm stats 同源：norm stats 数值进训练（归一化），两侧实际加载文件必须 sha256 相同——A 侧按 G0b `launch.md` 留档口径、B 侧按 G3 口径（已知同源，仍留档 sha256 比对结论）。

**A 侧正式跑**（worktree 内，tmux + AGENTS 7 三件套）：

```
入口：scripts/train.py（经 entry_equiv.py runpy 双跑）
argv 尾巴：mme_vla_suite --exp-name=entry-eq-a --overwrite
  --num-train-steps=20 --log-interval=1 --save-interval=10000
  --batch-size=8 --num-workers=4 --fsdp-devices=2 --no-wandb-enabled
  --model.use_history --model.history_config=perceptual-framesamp-context.yaml
  --dataset-path=$REPO/v1-store/datasets/4task-gl
  （assets/checkpoint 目录旗标照 G0b launch.md 留档口径，指到 v1-store；纯路径不进数值）
```

**B 侧正式跑**（本仓库，V5.0 tip、clean HEAD）：

```
入口：scripts/training/main.py（经 entry_equiv.py runpy 单跑）
argv 尾巴：train mme_vla_suite --exp-name=entry-eq-b
  --num-train-steps=20 --log-interval=1 --save-interval=10000
  --batch-size=8 --num-workers=4 --fsdp-devices=2 --no-wandb-enabled
  --model.use_history --model.history_config=perceptual-framesamp-context.yaml
  --dataset-path=$REPO/v1-store/datasets/4task-gl-framesamp
```

两侧 env 完全相同：`XLA_FLAGS='--xla_gpu_deterministic_ops=true --xla_gpu_autotune_level=0'`、`XLA_PYTHON_CLIENT_MEM_FRACTION=0.95`、`CUDA_VISIBLE_DEVICES=0,1`；编译缓存目录各侧独立（两侧计算图本就不同；跨编译 bitwise 由 D2-cold 授权）。argv 差异仅四处且全部登记：入口文件与 argv 头、`--exp-name`（只进 ckpt 路径与 wandb 名，不进数值路径，R6）、`--dataset-path`（链路差异的载体，正是被测对象）、`--overwrite`（仅 A 侧双跑需要）。数值口径逐项相同。

**比对与判定**：A 正式段 20 行 vs B 全程 20 行逐步五标量 hex 逐位；A 正式段末步 vs B 末步 TrainState 逐叶 sha；B 前 20 步另 vs G0b `scalars_hex.tsv` 前 20 行（前置 `uv run scripts/training/bench/check_baseline_env.py check` 出 `BASELINE_ENV=PASS`）。判定行见七节。

**时长与留档**：A 侧 `uv sync`（NFS copy 模式偏慢）+ 双跑冷编译约 30–60 min，B 侧约 20–40 min，全流程含留档预计半天。run_name 拟 `v1-entry-eq`（起跑前按 AGENTS 6 经用户确认）；留档 `docs/training-doc/v1-entry-eq/`（launch.md / result.md / records/，两侧 record jsonl 入 records/，A 侧 tentative 段单独存档）。PASS 后清理：worktree、两侧临时 ckpt 目录、`v1-store/entryeq/`（FAIL 记录按 `.failed-<n>` 保留）。

## 十、风险登记

| # | 风险 | 规避 |
|---|---|---|
| R1 | dataloader spawn worker 重导入 `__main__`（=main.py）时递归分发或重 import 重依赖 | main.py 模块体只有轻量定义 + `__main__` 守卫；进 worker 的对象全在 `mme_vla_suite.*` 包内按模块名 pickle |
| R2 | main.py 被误当训练链模块 import，破坏「入口 / 库」边界 | ENTRY_ISOLATION 判定行 + 顶部注释声明；pytest 经 importlib 按路径加载 |
| R3 | A 侧环境漂移导致位级失败误归因 | `uv.lock` 零 diff 实测在案；A 侧独立 `uv sync`；两侧 env.json 式留档逐项比对 |
| R4 | main 代码读 `4task-gl` 失败 | preflight P1 冒烟；失败即停、交用户，不得改 A 侧代码 |
| R5 | A/B argv 尾巴不可能逐字相同被质疑 | 差异仅四处且逐项登记（九节）；argv 管道正确性由 ENTRY_CONFIG_EQ 独立闸住 |
| R6 | 两侧 `--exp-name` 不同被质疑引入差异 | exp_name 只进 checkpoint 路径与 wandb 名，不进数值路径；如仍存疑改同名 + 清理后串行重跑 |
| R7 | A 有 tentative 段、B 没有，被质疑不对称 | tentative 独立初始化、状态全部丢弃（三节三处依据）；判据只比正式段；tentative 段留档不判 |
| R8 | wandb 补丁被 `wandb.init()` 盖掉致记录静默缺失 | `sys.modules["wandb"]` 代理预载手法（八节）；harness 启动即断言代理生效，记录行数与步数不符即 fail-loud |

## 十一、验证清单（commitV5.0 前逐条）与范围

**逐条验证**：七节判定行全绿（含 ENTRY_MAIN_EQ 全族与 STEPS=5 烟测）；`uv run pytest --collect-only -q src scripts packages` 零 error；ruff 零新增违例；`git status --short` 核对只含本轮 4 个新增文件。

**明确不做（需另拍板）**：① `compute_results.py` / `unzip_ckpt.py` 的 argparse→tyro 改写；② `finetune_mme_vla_suite.sh` / `eval.sh` / README 的规范命令是否切到 `main.py` 调用（属对外口径变更）；③ `train.py` 尾部双跑 `__main__` 块的删改（触碰冻结面，本轮不动）。
