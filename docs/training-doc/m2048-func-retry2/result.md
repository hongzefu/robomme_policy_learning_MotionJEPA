# 2048功能验收最终结果

全部V5功能门通过，最终以本run为准。clean源码e4a50672fc68770429654af4bc96c31e531a5195，GPU2，05:34:48Z→05:37:22Z，驱动154秒、检查器150.839秒，EXIT_CODE=0；会话自然退出，GPU释放。

补测实际确认loss_grads与loss_only在相同输入下有绝对差0.0007054805755615234，不能将该差值算作帧带作用。改用同一loss_only函数做三次A/A后逐位相同、噪声0；32个帧带各自相对此基线仍全部产生变化，最小delta=0.00010418891906738281。有效位垃圾负对照也在同一路径比较且结果有限。

其余原判据全部重新通过：三次loss及全部38个可训练梯度叶A/A一致、image和pos各2048位置非零、每帧删63位置的反例被拒；n=1/8/31的mask外垃圾不改变loss、固定noise动作或全部38叶梯度，无效位置输入梯度严格零、有效位置非零，向有效位注入垃圾改变loss。没有更改扰动幅度、阈值或排除任何叶。

完整[判定日志](records/driver.summary.log)、[压缩数值结果](records/func.json.gz)、[运行时来源](records/runtime.json)和[原SHA清单](records/archive_manifest.json)均保留。生产src树与CAND相同，依赖与复制权重身份沿用retry1核验；独立副本未改变主库三组在途训练。

用户要求「保持原计划，完整取证（推荐）」并授权并行；本次补测用于严格落实既定帧带与负对照判据。此前失败、探针及retry1记录原样保留，retry1的帧带/有效位基线由本次结果更新。此结论不替代真实训练对拍、checkpoint、容量与测速门，也不证明策略成功率提升。
