# 2048 八卡 1000 步测速结果

**SPEED_REPORT=READY，全部正确性与容量门通过，满足正式起跑条件。** 稳态 800 步耗时 824.5448 秒，平均 **1.030681 秒/步、124.1897 samples/s**；八卡 GPU 利用率均值 **98.6104%**，0% 采样占比 **0.1219%**。80k 中心估计 **23.0035 小时**，约 184.03 GPU·h；这是外推。

## 版本、配置与环境

PERF_HEAD 为 `9a7acc81b93855032a5f177a03adffe2c2209318`，起跑工作区 clean，期间代码和依赖不变。命令见 [launch.md](launch.md)，完整实测见 [report.json](records/report.json) 和 [launch.actual.json](records/launch.actual.json)。报告 SHA256 为 `0923badb8e438e4f9aa003eef0a6e99a8c204a5a38aa7cc4ee13be45adc6e4d6`，runner SHA256 为 `9702314d0aab880b4f5fee7ee1e6ea59052782be243f8d8ec4716e317f55802b`。

本机 8×A100-SXM4-80GB，驱动 595.71.05；底层 AWS 本地 NVMe RAID /dev/md0、XFS。batch128、worker16、FSDP8、prefetch_factor2、persistent_workers=true、pin_memory=false，生产 bf16。数据为同一 verified/full 1600ep framesamp-8x8，605611 个执行样本、1192918 帧；清单、norm_stats、YAML 摘要在报告中绑定。保持 mme_vla_suite_b128_80k 默认，仅覆盖 1000 步与关闭 W&B。

tmux 全名 `m2048-perf-20260921T051856Z`；driver UTC 2026-09-21 06:50:37 至 07:12:36，EXIT_CODE=0，训练 PID3966623、采样 PID3966615 均已退出。报告会话 `m2048-perf-report` 退出码也为0。末步 checkpoint 保留在主库 v1-store/train-runs/mme_vla_suite_b128_80k 下同名 run 的 999 目录，不入 Git。

## 稳态、GPU 与资源

预热 0..99、主窗 100..899、尾段 900..999 含真实保存。主窗同步边界 UTC 为 06:56:48.580319 至 07:10:33.125118；全程三处同步，没有 profiler、逐步摘要或逐步同步。

GPU 每500ms采样，各卡主窗1641点，平均间隔0.50227秒，最大0.557秒，时间覆盖约99.9003%。NVML内部周期限制仍在，重复值不作为独立新增证据。

| GPU | 利用率均值 % | 0% 采样占比 % | 显存峰值 MiB |
|---|---:|---:|---:|
| 0 | 98.4503 | 0.1828 | 81009 |
| 1 | 98.6124 | 0.1219 | 80995 |
| 2 | 98.5539 | 0.0609 | 80995 |
| 3 | 98.6027 | 0.0609 | 80995 |
| 4 | 98.7562 | 0.1828 | 80995 |
| 5 | 98.6569 | 0.0609 | 80995 |
| 6 | 98.6088 | 0.1219 | 80995 |
| 7 | 98.6417 | 0.1828 | 80995 |

慢步阈值1.545785秒，命中100、200、300、400、500、600、700、778、800。慢步GPU均值87.3664%（232个卡样本），其他步98.8127%（12896个卡样本）。显存峰值含JAX 0.95预分配，不能等同活跃张量大小。

共享内存峰值比0.106047，低于0.70。主窗data_next主机累计289.6446秒，占同步墙钟35.1278%；它可与异步GPU运算重叠，不能解释为GPU空闲或互斥耗时。train_dispatch主机累计529.4267秒。

主窗md0读取83054186496 B，全程114602827776 B，不能与逻辑读取混比。主窗Cached范围762898550784..846558109696 B，MemAvailable最小955332509696 B。训练树外观测IO共90165248 B，低于1GiB复核阈值，包含Codex与本轮tee、GPU采样写入。IO覆盖通过；约10秒的进程IO采样可能漏短命进程。

## 漂移与 80k 外推

八个100步块主机均值秒/步为1.027773、1.030856、1.029940、1.031157、1.030132、1.030398、1.032486、1.032694。早晚漂移比1.001013；尾段同步均值1.030254秒/步，无漂移门触发。

P=360.315234秒（初始化至99末同步），μ=1.030681秒/步，B=5.936539秒（保存调用阻塞），C=11.938040秒（保存开始至等待和收尾上界）。原计划公式 `P+79900μ+15B+C` 给出23.0035小时；保守保存 `P+79900μ+16C` 为23.0285小时。八块主机速度情景范围22.9390..23.0732小时，**不是统计置信区间或完整训练保证**。长期缓存演化、周期写盘争用、正式W&B与外部负载尚未由1000步复现；正式约300步再核对。

## 用户决定、结论与归档

用户原话：「1000步测速后如果占用率高于50% 并且没有其他的问题 可以直接启动训练」。按事先说明的800步稳态八卡总体均值，98.6104%严格高于50%；[全部正确性](../m2048-implementation/result.md)和[容量](../m2048-smoke/result.md)通过，无未解决问题。固定Beta后绑定报告、runner、配置与数据，执行V10并直接起跑已定正式run；授权来自测速前原话，条件成立的落档时间不冒充用户再次回复时间。

文件与SHA见 [archive_manifest.json](records/archive_manifest.json)：逐步计时、主机/GPU采样、指标、配置、报告、清洗日志与checkpoint元数据。gzip回读与原始SHA一致，两份清洗日志保留EXIT_CODE=0；SPEED_WINDOW、GPU_COVERAGE、SHM_PEAK全PASS。大权重留v1-store。高利用率不替代正确性、吞吐与资源成本结论。
