# v1-grad-baseline-g0b（G0b 黄金基线重跑，1000 步，P1b 后新版）

- **目的**：以 P1b 补遗后的量具重跑黄金基线，取代 300 步旧版 `v1-grad-baseline-g0`（经用户 2026-08-26 裁定：允许全部重跑、升级 1000 步、对拍通过后删除旧产物；原 P1b「补录 replay 轮」随之取消）。新产物原生含 canonical 输入摘要、index 序列与摘要步 TrainState 数组；新旧前 300 步 raw 口径逐位前缀对拍**兼作 P1b 量具改造（uv runner / canonical 通道 / `--save-interval 1` 步集机制）的等价性实证**。定义与登记簿权威载体：`v1-gradient-baseline.md`。
- **G0_SCOPE 断言（起跑前实测，PASS）**：锚点 `55e6e5bf8ef38b780902d0e63257ea859a432a2c` → `<G0b-HEAD>=570287ff64020285ef689e7ee9f0e0639ba4decd`（commitV2.3.1，P1b 量具补遗），`git diff --name-only` 全部落在白名单；`git status --porcelain` 空；submodule `third_party/robomme_benchmark` 指针 `856bc3a` 与锚点一致；两轮 env.json `git_dirty=false`。起跑父 commit 含期间插入的外部纯文档 commit `29c0dd7`（dtype 计划修订，白名单内 `.md`）。
- **preflight**：起跑前 `check_baseline_env.py check --baseline <旧 G0 round1>` 注入同口径环境变量后 `BASELINE_ENV=PASS`。
- **口径**：`bench_train_steps.py` 入口、2×RTX 6000 Ada、b8、**1000 步**、seed 42、`fsdp_devices=2`、`num_workers=4`、SAVE_INTERVAL=100 + `EXTRA_DIGEST_STEPS=299`（TrainState 摘要 @ 0/100/…/900/**299**/999 共 12 次，299 为对齐旧 G0 末步摘要的附加步）、输入摘要 @ 0/1/2/各摘要步（14 次，raw+canonical 双口径 + 逐步样本 index）、index 全序列、`XLA_FLAGS="--xla_gpu_deterministic_ops=true --xla_gpu_autotune_level=0"`（D2 档）、EXP_NAME=`v1-grad-baseline-g0b` 两轮共用（r2 编译缓存全命中）、`nvidia-smi -lms 500` + 15 s legacy 双通道采样。单 epoch 约束满足（1000×8=8,000 < 395,289）。
- **TrainState 数组落盘（仅 r1）**：`STATE_DUMP_STEPS=0,299,999`，每步 45.4 GiB（177 叶，裸字节容器）。经用户裁定迁存本机 `/data/hongzefu/v1-baselines/g0b-r1-state-dump/`（内附 README 与 `SHA256SUMS`，迁移时与 NFS 源逐文件 sha256 核对一致后删除 NFS 副本）；整文件 sha 清单进 git（`records/r1/state_dump.SHA256SUMS`）。用途：G1 对拍 `state_digest` 失配时 `compare_baseline.py --state-arrays-*` 的逐叶数值裁决参照。
- **命令**：`STEPS=1000 SAVE_INTERVAL=100 WORKERS=4 EXTRA_DIGEST_STEPS=299 [STATE_DUMP_STEPS=0,299,999] RUN_TAG=v1-grad-baseline-g0b-r{1,2} EXP_NAME=v1-grad-baseline-g0b KEEP_JAX_CACHE=1 XLA_FLAGS="…" bash scripts/smoke-local/run_2gpu_epoch_bench.sh`（完整 argv 见各轮 `records/r{1,2}/env.json`；run_name 起跑前经用户确认）
- ⚠ 红线 B7：本 run 带完整 TrainState 摘要 + 输入摘要 + 确定性档 + 数组落盘停顿，util/步时**仅留档参考、禁作任何性能结论**；性能锚点见 `docs/training-doc/v1-g0-speed-r2/`。
