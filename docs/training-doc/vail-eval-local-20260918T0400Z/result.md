# 首次本机启动：资源变化后停止

本次没有执行策略回合，不计为任务失败或成功。起跑 HEAD 为 4ceba91ca72d4864d0732d4ea801f68c8c015530；启动时检测输出显示 GPU 1 已被另一项 MotionJEPANewTask 占用约 7 GB，GPU 0 也被占用。启动命令把检查与启动放在同一调用中，未将空闲判定做成硬闸，这是本轮启动检查的遗漏。

确认后只向本次运行器及其 render_probe 的 timeout 进程发送 TERM，未触碰其他任务。来源检查已经通过，停止发生在渲染探针阶段，未启动 serve_policy、未运行 eval.py 策略回合。结束时间 2026-09-18T04:22:12Z，EXIT_CODE=143。原始日志见 records/run.log。

运行器随后补入本机 GPU 占用硬闸，在创建输出目录前和启动模型服务前检查。用户明确选择「等待空闲 GPU」，不允许同卡运行。后续采用全新运行名，保持原权重、任务、episode 和 seed，不覆盖本次证据。
