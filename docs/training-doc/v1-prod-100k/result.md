# v1-prod-100k 结果：TREND_OK=PASS，100k 步 134.7 h 跑完，util 99.1%，无衰减

> job 59620943 @ gl1520，2026-09-03 03:50 起跑、2026-09-08 18:37 结束（sacct `COMPLETED`，
> Elapsed 5-14:47:16）。起跑档 `launch.md`，提交实录 `records/submit.md`。
> wandb：https://wandb.ai/hongzefu-university-of-michigan/robomme-framesamp/runs/q38vcdzg

## 主结果

| 指标 | 实测 | 判据 |
|---|---|---|
| **util 均值**（dense 500 ms，稳态窗 [100, 99900]，n=3,868,788） | **99.089%** | ≥95% ✓ |
| **趋势下滑** | **+0.07pp**（后半段略高） | ≤1.0pp ✓ |
| `TREND_OK` | **PASS** | — |
| 0% 采样占比 / active_util / <100% 占比 | 0.8% / 99.9% / 2.6% | — |
| 四卡一致性 | 卡 0–3 全部 99.1% / 0.8% / 99.9% | — |
| 步时均值 | **4.849 s**（真实墙钟 483,885.5 s ÷ 99,800 步） | 起跑档外推 4.907 s，实测快 1.2% |
| 吞吐 | 13.20 样本/s（batch 64） | — |
| 训练墙钟 / walltime | **134:42:54 / 168 h**（用掉 80.2%，裕度 1.25×） | 起跑档预估 136.3 h ✓ |
| 显存峰值 | 43,749 MiB（`XLA_PYTHON_CLIENT_MEM_FRACTION=0.95` 预置池） | — |
| legacy 15 s 通道交叉 | util 均值 99.1%，dense−legacy = +0.0pp | 无采样偏差 |
| 附录 | util 中位 100%（勿作标题结论，AGENTS 16） | — |

100 步区间步时：min 4.778 s、max 5.039 s、中位 4.815 s，**无任何 >6 s 的慢区间**；100k 步全程
未出现 error / OOM / 重试。

## 分时趋势（每 1000 步一段，摘录；完整 100 段表见 slurm 日志 analyzer 输出）

| 步区间 | 步时均值 | util 均值 | 0% 占比 | 备注 |
|---|---|---|---|---|
| 100–6000 | 4.91–4.94 s | 97.2–97.6% | 2.0–2.5% | 起跑首段偏低带 |
| 6000–12000 | 4.80 s | 99.4–99.7% | 0.2–0.5% | — |
| 12000–18000 | 4.88–4.92 s | 97.3–98.0% | 1.8–2.4% | 第二偏低带 |
| 18000–50000 | 4.79–4.86 s | 98.8–100% | 0–1.0% | 最稳段 |
| 50000–56000 | 4.90–4.95 s | 98.4–99.0% | 0.6–1.2% | — |
| 56000–80000 | 4.79–4.88 s | 99.4–100% | 0–0.5% | — |
| 80000–92000 | 4.91–4.97 s | 96.9–97.7% | 2.0–2.8% | 第三偏低带（最长，约 16 h） |
| 92000–99900 | 4.80–4.83 s | 99.9% | 0–0.1% | 收尾恢复 |

判读：util 在 97% 与 99.9% 两个台阶之间**分带切换**，带宽以小时计、切换无渐变，且后半段不低于
前半段（drop=+0.07pp），**不是随时间累积的衰减**。97% 带内步时同步慢约 2.5%（4.92 vs 4.80 s），
0% 采样占比同步升到 2–2.8%，即每步有约 120 ms 的 GPU 空转——与「同节点其余 4 张 A40 被其他
job 占用时 CPU / PCIe / NFS 竞争」的形态一致（gl1520 为 8 卡节点，本 job 只占 4 卡 16 CPU），但
本 run 未采集同节点他人 job 信息，**归因只到形态相似，不下定论**。对总时长的影响上界：全部
100k 步按 97% 带步时算也只多 2.5%（≈3.4 h），在 168 h 裕度内。

## 语义前缀核对：与 v1-prod-60k 同 seed 逐里程碑对照

两 run 同 seed 42、同配置、同数据顺序，理论上前 60k 步是同一条训练轨迹的语义前缀（跨节点非位级）。
`metrics.jsonl`（每 100 步区间均值）逐里程碑对照：

| step | 100k：loss / grad_norm | 60k：loss / grad_norm |
|---|---|---|
| 0 | 0.63757 / 39.78 | 0.63711 / 40.20 |
| 10000 | 0.00529 / 0.0444 | 0.00518 / 0.0435 |
| 20000 | 0.00322 / 0.0319 | 0.00323 / 0.0320 |
| 30000 | 0.00250 / 0.0292 | 0.00241 / 0.0278 |
| 40000 | 0.00202 / 0.0290 | 0.00209 / 0.0330 |
| 50000 | 0.00170 / 0.0275 | 0.00168 / 0.0287 |
| 59900 | 0.00152 / 0.0254 | 0.00151 / 0.0267 |
| 70000 | 0.00128 / 0.0234 | — |
| 80000 | 0.00126 / 0.0260 | — |
| 90000 | 0.00108 / 0.0227 | — |
| 99900 | **0.00109 / 0.0260** | — |

前 60k 步 loss 逐里程碑相对差 ≤4%（step 0 起的 bf16 非确定性累积范围内），**语义前缀主张成立**。
60k→100k 段 loss 从 0.00152 继续降到 0.00109（−28%），80k 后趋平（0.00126 → 0.00108 → 0.00109），
grad_norm 在 0.023–0.026 波动；param_norm 从 1803.1 单调升到 1867.0。

## checkpoint 停顿与内存

| 保存点 | 该 100 步区间耗时 | 相对常规中位 481.5 s 多出 |
|---|---|---|
| 10000 / 20000 / 30000 | 486.6 / 495.3 / 483.2 s | +5.1 / +13.8 / +1.6 s |
| 40000 / 50000 / 60000 | 489.6 / 498.1 / 497.4 s | +8.1 / +16.6 / +15.9 s |
| 70000 / 80000 / 90000 | 503.9 / 486.2 / 503.0 s | +22.4 / +4.6 / +21.5 s |

九次异步保存合计多出 ≈110 s，占全程 0.02%；末份 `99999` 同步落盘（blocking 6 s，1.9 GiB/s）。
**10 份 ckpt 全部保留**（`10000 … 90000, 99999`，`keep_period=10000`），合计 **129 GB**，单份 13 GB。
末份目录名 `99999` 与 60k 的 `59999` 同规律（训练循环在第 `num_train_steps-1` 步落最后一份）。

| 内存项 | 实测 |
|---|---|
| cgroup **anon 峰值** | **49.13 GiB**（128G 的 38.4%；trend-10h 为 49.79 GiB，一致） |
| 档位判据 ≤0.6×mem = 76.8 GiB | ✓ |
| 节点级 Cached 峰值 | 330.8 GiB（节点共享页缓存，可回收，不计入档位） |
| pgmajfault 全程增量 | 23,871（134.7 h） |
| NFS server_read 实测 | 5 MB/s（36 GB packed 库进页缓存后近乎零读盘） |

## 两次提交的教训（详见 `records/submit.md`）

首次提交 59473806 排队 5 h 后被 `EXPECTED_GIT_HEAD` 起跑闸拦下（另一会话的纯 docs 提交让 HEAD
漂移），且发现闸对未跟踪 `??` 文件同样生效。用户拍板去闸重提；第二次排队 17.5 h 起跑。
provenance 改由 env.json 起跑时自动记录的 `git_head=6891f69` 承担；训练代码自 commitV5.1
`baeaee7` 起零改动。

## 产物

| 产物 | 位置 | 进 git |
|---|---|---|
| 10 份 checkpoint（129 GB） | `v1-store/train-runs/mme_vla_suite/v1-prod-100k/{10000,…,90000,99999}` | 否 |
| 全分辨率记录（dense 151 MB 等） | `v1-store/train-records/v1-prod-100k/` | 否 |
| slurm 日志（39,252 行） | `v1-store/logs/v1-prod-100k-59620943.log` | 否 |
| 降采样与小体积记录 | `records/`（见 `records/README.md`） | 是 |
| wandb run | `q38vcdzg` @ `robomme-framesamp` | — |

## 结论

1. 100k 步正式训练一次跑通，`TREND_OK=PASS`，util 均值 99.1%、无衰减，步时 4.849 s 比起跑档外推
   还快 1.2%，168 h walltime 用掉 80%。
2. 前 60k 步与 `v1-prod-60k` 逐里程碑吻合，语义前缀成立；60k→100k 段 loss 再降 28%，80k 后趋平。
3. 三段约 97% 的 util 偏低带形态指向同节点邻居竞争，未归因定论，对总时长影响上界 2.5%。
4. 待办：`ckpt 99999` 在线评估（与 `v1-prod-60k/ckpt59999` 同口径对照，评估留档另开）；
   `v1-prod-60k` 尚缺 `result.md`（其 `TREND_OK=FAIL drop=+1.13pp` 未留档判读）。
