# 重跑状态

本批次尚未起跑。a批次在初始化201叶摘要615.913秒后完成前两步更新，随后为优化取证主动中断，阶段EXIT_CODE=130、队列EXIT_CODE=1；保留原现场，不认定20步验证通过。

取证优化保持每叶原dtype、shape和C序字节；最多8叶并行读取及SHA256，主线程按原树遍历顺序回收结果和写盘。训练计算、输入、优化器及正式超参不改。

新增21项CPU检查通过（0.43秒），覆盖原字节协议、标量/空数组/负stride/bfloat16、并行返回顺序、dump字节和原记录验证器。另实跑8张GPU分片的25叶并行拷贝，原始字节与SHA全等，CHECKSUM_8GPU=PASS，取证阶段0.653秒；这是小数组协议验证，不是完整TrainState的加速结论。

原20+1步、201叶、八卡b128/w16/FSDP8、100步保存加载、容量20步、测速1000步及4096→1024正式队列全部保留。首份完整状态产生后与a批次的201叶初态逐项核对，并以真实耗时确认优化效果。

与旧_common摘要协议的交叉检查1 passed（6.45秒）；顺序队列、shell错误传播及实际临时文件归档检查17 passed（0.23秒），压缩GPU记录可无损读回，未归档任何.bin/.yaml/.sh。W&B只读认证查询通过，未输出凭据或用户名。
