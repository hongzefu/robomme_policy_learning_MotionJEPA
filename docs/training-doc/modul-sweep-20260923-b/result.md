# 2048完整回归通过，队列在4096前暂停

改前、改后、改后开启最终保存记录三组均完成20+1步，完整状态0–20、每状态201叶、20批输入和五标量逐位一致；两个对照均mismatches=0，各自7个记录损坏反例均拒绝。九个验证阶段全部退出0。整个调度器在比较通过后按预定边界SIGINT退出130，未进入4096验证或正式训练。

起跑/取证器为 `5e97d52a6420fdfd85620773b0496526bbf589a4`，旧训练源码为 `742f2d894d25abe802a26b1c118852b0b7676e44`；所有六个run使用同一4份取证相关源码和7项设置（8线程、记录间隔1、三个记录开关开启、附加步和落盘步集合为空）。新增检查器在开发工作区提前重读全部六份原记录，也得到RECORDER_MATCH=PASS；这些method-check文件不是新训练启动版本的声明，c批次将从新Beta重验原记录与环境指纹。

| 组别 | 步数 | 实际阶段秒数 | 退出码 |
|---|---:|---:|---:|
| before | 20 | 3030.327 | 0 |
| before | 1 | 420.060 | 0 |
| after | 20 | 2612.111 | 0 |
| after | 1 | 510.122 | 0 |
| after-final | 20 | 3344.519 | 0 |
| after-final | 1 | 461.488 | 0 |

开始UTC 2026-09-23T01:11:22.673397+00:00，最终比较通过UTC 2026-09-23T04:04:34.141714+00:00。本机8×A100-SXM4-80GB、/dev/md0 XFS NVMe RAID，b128/w16/FSDP8/seed42；仅验证记录器的CPU worker为8。以上耗时包含完整取证和初始化，不能外推为正式训练速度。两个正式80k run均未启动，ETA仍待各自1000步独立测速。

## 用户决定及计划外事件

用户原话：「/scratch/hongze/robomme_policy_learning_MotionJEPA/0922-4096-1024-8gpu-training-plan.md 开始做 有问题立刻问用户 起泡后给出预计的时间」。

此前在收到用户「保持现有实现继续验证」的回复前，已将取证改成8线程并启动b批次；这一步推进过早，已向用户说明。用户随后选择「继续当前验证，停止后续优化」，最终以以下决定覆盖先前选择：

用户最终决定：「可以优化取证方式 但要保证改前后都是用的一种取证方式 这个是用户最终决策」。

因此本轮保持b的同一取证实现完成六个run，不把a串行记录与b并行记录拼接。a与b初态及短前缀相同只作为取证协议检查，原a中断记录保留。

等待b运行期间发现4096选帧验证器错误采用理想整数floor，实际生产一直使用float64步长后取整；真实帧号0–2303中64帧档有67处不同。修正只在独立开发副本准备，等本次回归全部通过才暂停调度并合入主副本；生产采样和训练数学没有改变。临时归档程序首次把队列日志误当成单阶段日志，因九个嵌套退出0加最终130而拒绝；已按完整预期序列校验并原样保留全部退出记录，没有删除或改写运行日志。

## 文件与后续

逐run记录在对应子档案，整体records包含起跑信息、展开命令与事件、两个完整比较、同方法补强核验、最终用户决定、边界触发记录与清洗队列日志。源脚本和YAML从启动提交还原；权重不进Git。保存记录开关回归不代替真实checkpoint验收。

后续[c批次](../modul-sweep-20260923-c/launch.md)先重验代码差异白名单、同一取证方式、环境和数据指纹，并重读本组全部原记录，再进入4096输入、训练对拍、保存加载、容量、测速和正式训练，4096完成验收后才进入1024。
